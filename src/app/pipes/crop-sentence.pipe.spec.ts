import { CropSentencePipe } from './crop-sentence.pipe';

describe('CropSentencePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CropSentencePipe();
    expect(pipe).toBeTruthy();
  });
});
